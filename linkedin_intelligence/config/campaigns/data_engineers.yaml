campaign:
  name: Data Engineers
  objective: activated_signup

target:
  segments:
    - data_high_icp
  max_per_company: 2

strategy:
  primary: problem_first
  alternatives:
    - conversation_first
    - warm_relationship

limits:
  max_new_prospects_per_batch: 10
  min_evidence_confidence: 0.5
  min_priority: 40
  contact_cooldown_days: 60

sequence:
  follow_up_days: [3, 7]
  max_follow_ups: 2

approval:
  required: true

attribution:
  source: linkedin
